import { Manifold } from "../../300_manifold/[manifold]";
import { Fluid } from "../[fluid]";

export let FluidManifold = () => {
    
        return (
            <Fluid>
                <Manifold />
            </Fluid>
        );
    
    }
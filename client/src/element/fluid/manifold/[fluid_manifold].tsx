import { Manifold } from "../../manifold/[manifold]";
import { Fluid } from "../[fluid]";

export let FluidManifold = () => {
    
        return (
            <Fluid>
                <Manifold />
            </Fluid>
        );
    
    }
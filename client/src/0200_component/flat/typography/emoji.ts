// TODO: more flexible approach using meoization and caching
// + support any emoji or unicode character
// + detect if characters are not supported by drei text component
//      + render a texture with the character
//      + add mesh to text node
//          + use texture for mesh child of text node
//          + position mesh to match character position
// + cache textures

export {}